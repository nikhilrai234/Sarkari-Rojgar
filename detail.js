async function loadDetail(){const params=new URLSearchParams(window.location.search);const id=parseInt(params.get('id'));const res=await fetch('jobs.json');const data=await res.json();const job=data.find(j=>j.id===id);const container=document.getElementById('jobDetail');if(!job){container.innerHTML='<h2>Job not found</h2>';return;}container.innerHTML=`<h2>${job.title}</h2><p><strong>Date:</strong> ${job.date}</p><p><strong>Category:</strong> ${job.tag}</p><p>${job.details}</p><a href="index.html" class="page-btn">← Back to List</a>`;}document.addEventListener('DOMContentLoaded',loadDetail);